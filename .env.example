# Copy this file to .env.local (do NOT commit .env.local) and fill the secrets.
# Values prefixed with NEXT_PUBLIC are exposed to the browser (avoid putting secrets there).

# Runtime
NODE_ENV=development
HOSTURL=http://localhost:3000

# Auth (Simple token-based)
SIMPLE_AUTH_TOKEN=test_token_123

# Database (Drizzle / Neon)
# The codebase references multiple names; set whichever your setup expects.
DRIZZLE_DB_URL=
DRIZZLE_Database_URL=
NEXT_PUBLIC_DRIZZLE_DB_URL=

# Stripe (payments)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
NEXT_PUBLIC_STRIPE_SECRET_KEY=

# Gemini / AI provider
NEXT_PUBLIC_GEMINI_API_KEY=

# Feature toggles / app settings
NEXT_PUBLIC_INTERVIEW_QUESTION_COUNT=5

# Add any other provider keys below as needed (e.g., FIREBASE, SUPABASE, OPENAI)
# FIREBASE_API_KEY=
# SUPABASE_URL=
# SUPABASE_KEY=
# OPENAI_API_KEY=

# Notes:
# - Keep secrets (server-only keys) out of NEXT_PUBLIC_* variables.
# - After filling .env.local, restart the dev server to pick up changes.
